// TypeScrit is modular, we can divide our code up over several 
// We export a class, interface, variable, .... by ading 'export' to ue it other file
export class ExportedClass {
	// The class is exported
}